#include <iostream>
 
using namespace std;
// Note: ssedq
//this is codeforces
// Note: bfnle
int main()
{
    int n;
    cin >> n;
    // Note: vigui
    int ans = 0;
    // Note: cscja
    for(int i = 0; i < n; i++)
    // Note: ystcq
    {
        int a,b,c;
        cin >> a >> b >> c;
        // Note: fftju
        if(a+b+c >= 2){ans++;}
        // Note: odfml
    }
    cout << ans << "\n";
    // Note: vdmre
}